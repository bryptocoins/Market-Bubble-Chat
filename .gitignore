node_modules/
.next/
dist/
build/
*.log

# electron-builder output
desktop/dist/
desktop/release/

# local config / secrets
.env
.env.local
server/data/
server/.env
web/.env.local

# playwright profile (X session)
x-profile/
.x-profile/
server/.x-profile/

# machine-specific launcher shortcut (regenerate locally; the .bat is portable)
*.lnk

# os
.DS_Store
Thumbs.db

# build-freshness marker (auto-rebuild after pulls)
.buildrev
