# Frontend → backend connection. Defaults work for local dev (backend on :4000).
# Override when hosting the backend elsewhere.
# NEXT_PUBLIC_WS_URL=ws://localhost:4000
# NEXT_PUBLIC_CONFIG_URL=http://localhost:4000/config
